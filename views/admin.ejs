<!DOCTYPE html>
<html>
<head>
    <title>Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        :root {
            --brand-500: #C68148;
            --brand-600: #a66d3a;
            --brand-100: #f3e7db;
            --bg-50: #f8f9fa;
            --card-bg: #ffffff;
            --text-900: #333333;
            --text-600: #6c757d;
            --border-200: #e9ecef;
            --shadow-sm: 0 2px 8px rgba(0,0,0,0.06);
            --shadow-md: 0 8px 24px rgba(0,0,0,0.08);
            --radius-md: 12px;
            --radius-lg: 16px;
            --trans-fast: 180ms ease;
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', 'Segoe UI', 'Roboto', Arial, sans-serif;
            background: var(--bg-50);
            color: var(--text-900);
            overflow-x: hidden;
        }
        
        .admin-container {
            display: flex;
            min-height: 100vh;
        }
        
        /* Sidebar */
        .sidebar {
            width: 280px;
            background: var(--card-bg);
            padding: 20px 0;
            position: fixed;
            height: 100vh;
            overflow-y: auto;
            z-index: 1000;
            border-right: 1px solid var(--border-200);
            box-shadow: var(--shadow-sm);
        }
        
        .logo {
            padding: 0 20px 30px;
            border-bottom: 1px solid #e9ecef;
            margin-bottom: 30px;
        }
        
        .logo h2 {
            color: var(--brand-500);
            font-size: 24px;
            font-weight: 700;
        }
        
        .nav-menu {
            list-style: none;
            padding: 0 15px;
        }
        
        .nav-item {
            margin-bottom: 8px;
        }
        
        .nav-link {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            color: var(--text-600);
            text-decoration: none;
            border-radius: var(--radius-md);
            transition: transform var(--trans-fast), background var(--trans-fast), color var(--trans-fast), box-shadow var(--trans-fast);
            font-weight: 500;
        }
        
        .nav-link:hover, .nav-link.active {
            background: var(--brand-500);
            color: #ffffff;
            box-shadow: var(--shadow-sm);
            transform: translateY(-1px);
        }
        
        .nav-link i {
            margin-right: 12px;
            width: 20px;
            text-align: center;
        }
        
        .upgrade-card {
            margin: 20px 15px;
            background: linear-gradient(135deg, var(--brand-500), #d4a574);
            padding: 20px;
            border-radius: var(--radius-md);
            text-align: center;
            color: white;
            box-shadow: var(--shadow-sm);
        }
        
        .upgrade-card h4 {
            font-size: 14px;
            margin-bottom: 8px;
        }
        
        .upgrade-card p {
            font-size: 12px;
            opacity: 0.9;
            margin-bottom: 15px;
        }
        
        .upgrade-btn {
            background: rgba(255,255,255,0.2);
            border: none;
            color: white;
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 12px;
            cursor: pointer;
            transition: background var(--trans-fast), transform var(--trans-fast);
        }
        
        .upgrade-btn:hover {
            background: rgba(255,255,255,0.3);
            transform: translateY(-1px);
        }
        
        /* Main Content */
        .main-content {
            flex: 1;
            margin-left: 280px;
            padding: 20px;
        }
        
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding: 0 10px;
        }
        
        .search-bar {
            position: relative;
            width: 300px;
        }
        
        .search-bar input {
            width: 100%;
            padding: 12px 40px 12px 15px;
            background: #ffffff;
            border: 1px solid #e9ecef;
            border-radius: 25px;
            color: #333333;
            font-size: 14px;
        }
        
        .search-bar input::placeholder {
            color: #6c757d;
        }
        
        .search-bar i {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
        }
        
        .user-profile {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .notification {
            position: relative;
            color: #6c757d;
            font-size: 18px;
            cursor: pointer;
        }
        
        .notification::after {
            content: '';
            position: absolute;
            top: -2px;
            right: -2px;
            width: 8px;
            height: 8px;
            background: #C68148;
            border-radius: 50%;
        }
        
        .profile-info {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .profile-pic {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #C68148;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
        }
        
        .profile-name {
            color: #333333;
            font-weight: 500;
        }
        
        .dropdown {
            color: #6c757d;
            cursor: pointer;
        }
        
        /* Compact Date Filter */
        .date-filter {
            background: var(--card-bg);
            border-radius: var(--radius-md);
            padding: 20px;
            margin-bottom: 30px;
            border: 1px solid var(--border-200);
            box-shadow: var(--shadow-sm);
        }
        
        .date-filter h3 {
            color: #333333;
            font-size: 16px;
            margin-bottom: 15px;
            font-weight: 600;
        }
        
        .filter-form {
            display: flex;
            gap: 15px;
            align-items: end;
            flex-wrap: wrap;
        }
        
        .filter-group {
            display: flex;
                flex-direction: column;
            gap: 5px;
        }
        
        .filter-group label {
            color: var(--text-600);
            font-size: 12px;
            font-weight: 500;
        }
        
        .filter-group input,
        .filter-group select {
            padding: 8px 12px;
            background: var(--card-bg);
            border: 1px solid var(--border-200);
            border-radius: 8px;
            color: var(--text-900);
            font-size: 14px;
            width: 140px;
            transition: border-color var(--trans-fast), box-shadow var(--trans-fast);
        }
        
        .filter-group input:focus,
        .filter-group select:focus {
            outline: none;
            border-color: var(--brand-500);
            box-shadow: 0 0 0 3px rgba(198, 129, 72, 0.15);
        }
        
        .filter-buttons {
            display: flex;
            gap: 10px;
        }
        
        .btn-primary {
            background: var(--brand-500);
            border: none;
            color: white;
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            transition: background var(--trans-fast), transform var(--trans-fast), box-shadow var(--trans-fast);
            box-shadow: var(--shadow-sm);
        }
        
        .btn-primary:hover {
            background: var(--brand-600);
            transform: translateY(-1px);
            box-shadow: var(--shadow-md);
        }
        
        .btn-secondary {
            background: #f8f9fa;
            border: 1px solid var(--border-200);
            color: var(--text-600);
            padding: 8px 16px;
            border-radius: 8px;
            font-size: 14px;
            cursor: pointer;
            transition: background var(--trans-fast), color var(--trans-fast), transform var(--trans-fast);
        }
        
        .btn-secondary:hover {
            background: #e9ecef;
            color: var(--text-900);
            transform: translateY(-1px);
        }
        
        .filter-summary {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 12px 15px;
            margin-top: 15px;
            color: #856404;
            font-size: 14px;
        }
        
        .filter-summary strong {
            color: var(--brand-500);
        }
        
        /* Stats Cards */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .stat-card {
            background: var(--card-bg);
            border-radius: var(--radius-md);
            padding: 20px;
            border: 1px solid var(--border-200);
            position: relative;
            overflow: hidden;
            box-shadow: var(--shadow-sm);
            transition: transform var(--trans-fast), box-shadow var(--trans-fast);
        }
        
        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, var(--brand-500), #d4a574);
        }
        .stat-card:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }
        
        .stat-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }
        
        .stat-icon {
            width: 40px;
            height: 40px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
        }
        
        .stat-icon.revenue {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
        }
        
        .stat-icon.bookings {
            background: rgba(59, 130, 246, 0.1);
            color: #3b82f6;
        }
        
        .stat-icon.avg-booking {
            background: rgba(245, 158, 11, 0.1);
            color: #f59e0b;
        }
        
        .stat-icon.occupancy {
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
        }
        
        .stat-icon.hotels {
            background: rgba(168, 85, 247, 0.1);
            color: #a855f7;
        }
        
        .stat-value {
            font-size: 24px;
            font-weight: 700;
            color: var(--text-900);
            margin-bottom: 5px;
        }
        
        .stat-label {
            color: var(--text-600);
            font-size: 14px;
            margin-bottom: 10px;
        }
        
        .stat-change {
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 12px;
            font-weight: 500;
        }
        
        .stat-change.positive {
            color: #22c55e;
        }
        
        .stat-change.negative {
            color: #ef4444;
        }
        
        .mini-chart {
            position: absolute;
            bottom: 15px;
            right: 15px;
            width: 60px;
            height: 30px;
            opacity: 0.3;
        }
        
        /* Charts Section */
        .charts-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 30px;
        }
        
        .chart-card {
            background: var(--card-bg);
            border-radius: var(--radius-md);
            padding: 20px;
            border: 1px solid var(--border-200);
            box-shadow: var(--shadow-sm);
            transition: transform var(--trans-fast), box-shadow var(--trans-fast);
        }
        .chart-card:hover { box-shadow: var(--shadow-md); }
        
        .chart-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .chart-title {
            color: #333333;
            font-size: 18px;
            font-weight: 600;
        }
        
        .chart-controls {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .chart-period {
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            color: #6c757d;
            padding: 6px 12px;
            border-radius: 6px;
            font-size: 12px;
            cursor: pointer;
        }
        
        .chart-add {
            background: #C68148;
            border: none;
            color: white;
            width: 32px;
            height: 32px;
            border-radius: 6px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .chart-container {
            position: relative;
            height: 300px;
        }
        
        /* Tables */
        .table-card {
            background: var(--card-bg);
            border-radius: var(--radius-md);
            padding: 20px;
            border: 1px solid var(--border-200);
            margin-bottom: 20px;
            box-shadow: var(--shadow-sm);
        }
        
        .table-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .table-title {
            color: #333333;
            font-size: 18px;
            font-weight: 600;
        }
        
        .view-all {
            color: #C68148;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
        }
        
        .view-all:hover {
            text-decoration: underline;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #e9ecef;
        }
        
        th {
            color: var(--text-600);
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        td {
            color: var(--text-900);
            font-size: 14px;
        }
        
        tr:hover {
            background: rgba(198, 129, 72, 0.06);
        }

        /* Smooth scrollbar in sidebar */
        .sidebar::-webkit-scrollbar { width: 8px; }
        .sidebar::-webkit-scrollbar-track { background: transparent; }
        .sidebar::-webkit-scrollbar-thumb { background: #ddd; border-radius: 6px; }
        
        .user-info {
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .user-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: #C68148;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 12px;
            font-weight: 600;
        }
        
        .user-details h4 {
            color: #333333;
            font-size: 14px;
            margin-bottom: 2px;
        }
        
        .user-details p {
            color: #6c757d;
            font-size: 12px;
        }
        
        /* Responsive */
        @media (max-width: 1200px) {
            .charts-section {
                grid-template-columns: 1fr;
            }
            .stats-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }
        
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
            }
            
            .main-content {
                margin-left: 0;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .filter-form {
                flex-direction: column;
                align-items: stretch;
            }
            
            .filter-group {
                width: 100%;
            }
            
            .filter-group input,
            .filter-group select {
                width: 100%;
            }
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<%- layout('./layouts/boilerplate') %>
<body>
<div class="admin-container">
    <!-- Sidebar -->
    <div class="sidebar">
        <div class="logo">
            <h2>TravelNest</h2>
        </div>
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="/admin" class="nav-link active">
                    <i class="fas fa-chart-pie"></i>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="/listings" class="nav-link">
                    <i class="fas fa-hotel"></i>
                    Hotels
                </a>
            </li>
            <li class="nav-item">
                <a href="/profile" class="nav-link">
                    <i class="fas fa-user"></i>
                    Bookings
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin?filterType=createdAt&startDate=<%= new Date().toISOString().split('T')[0] %>" class="nav-link">
                    <i class="fas fa-calendar-day"></i>
                    Today
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin?filterType=createdAt&startDate=<%= new Date(Date.now() - 7*24*60*60*1000).toISOString().split('T')[0] %>" class="nav-link">
                    <i class="fas fa-calendar-week"></i>
                    This Week
                </a>
            </li>
            <li class="nav-item">
                <a href="/admin?filterType=createdAt&startDate=<%= new Date(Date.now() - 30*24*60*60*1000).toISOString().split('T')[0] %>" class="nav-link">
                    <i class="fas fa-calendar-alt"></i>
                    This Month
                </a>
            </li>
        </ul>
        <div class="upgrade-card" style="background: #f8f9fa; border: 1px solid #e9ecef;">
            <h4 style="color: #333333;">Quick Actions</h4>
            <p style="color: #6c757d;">Manage your bookings</p>
            <a href="/listings/new" class="upgrade-btn" style="text-decoration: none; display: inline-block; background: #C68148;">Add Hotel</a>
        </div>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Header -->
        <div class="header">
            <h1 style="color: #333333; font-size: 28px; font-weight: 700; margin: 0;">Admin Dashboard</h1>
            <div class="user-profile">
                <div class="profile-info">
                    <div class="profile-pic"><%= currentUser ? currentUser.username.charAt(0).toUpperCase() : 'A' %></div>
                    <div class="profile-name"><%= currentUser ? currentUser.username : 'Admin' %></div>
                    <a href="/logout" style="color: #6c757d; text-decoration: none; margin-left: 10px;">
                        <i class="fas fa-sign-out-alt"></i>
                    </a>
                </div>
            </div>
        </div>

        <!-- Compact Date Filter -->
        <div class="date-filter">
            <h3>Date Filter</h3>
            <form id="dateFilterForm" method="GET" action="/admin" class="filter-form">
                <div class="filter-group">
                    <label for="startDate">From Date</label>
                    <input type="date" id="startDate" name="startDate" value="<%= startDate || '' %>">
                </div>
                <div class="filter-group">
                    <label for="endDate">To Date</label>
                    <input type="date" id="endDate" name="endDate" value="<%= endDate || '' %>">
                </div>
                <div class="filter-group">
                    <label for="filterType">Filter By</label>
                    <select id="filterType" name="filterType">
                        <option value="createdAt" <%= (filterType === 'createdAt') ? 'selected' : '' %>>Booking Date</option>
                        <option value="checkIn" <%= (filterType === 'checkIn') ? 'selected' : '' %>>Check-in Date</option>
                        <option value="checkOut" <%= (filterType === 'checkOut') ? 'selected' : '' %>>Check-out Date</option>
                    </select>
                </div>
                <div class="filter-buttons">
                    <button type="submit" class="btn-primary">Apply</button>
                    <button type="button" onclick="clearFilter()" class="btn-secondary">Clear</button>
                </div>
            </form>
            
            <!-- Filter Summary -->
            <% if (startDate || endDate) { %>
                <div class="filter-summary">
                    <strong>Active Filter:</strong> 
                    <% if (filterType === 'createdAt') { %>
                        Bookings created
                    <% } else if (filterType === 'checkIn') { %>
                        Check-in dates
                    <% } else if (filterType === 'checkOut') { %>
                        Check-out dates
                    <% } %>
                    <% if (startDate && endDate) { %>
                        from <strong><%= new Date(startDate).toLocaleDateString() %></strong> to <strong><%= new Date(endDate).toLocaleDateString() %></strong>
                    <% } else if (startDate) { %>
                        from <strong><%= new Date(startDate).toLocaleDateString() %></strong> onwards
                    <% } else if (endDate) { %>
                        up to <strong><%= new Date(endDate).toLocaleDateString() %></strong>
                    <% } %>
                    | <strong><%= stats.bookings %></strong> bookings found
                </div>
            <% } %>
        </div>

        <!-- Stats Cards -->
        <div class="stats-grid">
        <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon revenue">
                        <i class="fas fa-rupee-sign"></i>
                    </div>
                </div>
                <div class="stat-value">₹<%= stats.revenue %></div>
                <div class="stat-label">Total Revenue</div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    +12.5%
                </div>
                <div class="mini-chart">
                    <canvas id="revenueChart"></canvas>
            </div>
        </div>
        <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon bookings">
                        <i class="fas fa-chart-line"></i>
                    </div>
                </div>
                <div class="stat-value"><%= stats.bookings %></div>
                <div class="stat-label">Total Bookings</div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    +1.4%
                </div>
                <div class="mini-chart">
                    <canvas id="bookingsChart"></canvas>
=======
<%- layout('./layouts/boilerplate') %>

<link rel="stylesheet" href="/css/admin.css">

<div class="admin-wrapper">
  <!-- Sidebar -->
  <div class="admin-sidebar" id="adminSidebar">
    <div class="sidebar-header">
      <h3><i class="fas fa-tachometer-alt me-2"></i>Admin Panel</h3>
    </div>
    <nav class="sidebar-nav">
      <div class="nav-item">
        <a href="/admin" class="nav-link active" data-page="dashboard">
          <i class="fas fa-chart-pie"></i>
          <span>Dashboard</span>
        </a>
      </div>
      <div class="nav-item">
        <a href="/admin/hotels" class="nav-link" data-page="hotels">
          <i class="fas fa-hotel"></i>
          <span>Hotel Management</span>
        </a>
      </div>
      <div class="nav-item">
        <a href="/admin/users" class="nav-link" data-page="users">
          <i class="fas fa-users"></i>
          <span>User Management</span>
        </a>
      </div>
      <div class="nav-item">
        <a href="/listings" class="nav-link">
          <i class="fas fa-home"></i>
          <span>Back to Home</span>
        </a>
      </div>
      <div class="nav-item" style="margin-top: 2rem; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 1rem;">
        <a href="/logout" class="nav-link">
          <i class="fas fa-sign-out-alt"></i>
          <span>Logout</span>
        </a>
      </div>
    </nav>
  </div>

  <!-- Main Content -->
  <div class="admin-content" id="adminContent">
    <div class="admin-page py-4">
      <div class="admin-header mb-4">
        <div class="d-flex align-items-center justify-content-between">
          <div>
            <h1 class="h3 mb-1">Dashboard Overview</h1>
            <p class="text-muted mb-0">Revenue, bookings and performance metrics</p>
          </div>
          <div>
            <span class="badge bg-success">Online</span>
          </div>
        </div>
      </div>

      <div class="row g-4 stat-cards mb-4">
        <div class="col-12 col-sm-6 col-xl-3">
          <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center">
              <div class="icon bg-revenue me-3"><i class="fas fa-rupee-sign"></i></div>
              <div>
                <div class="label text-muted">Total Revenue</div>
                <div class="value h4 mb-0">₹<%= stats.revenue %></div>
              </div>
>>>>>>> Stashed changes
            </div>
          </div>
        </div>
<<<<<<< Updated upstream
        <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon avg-booking">
                        <i class="fas fa-rupee-sign"></i>
                    </div>
                </div>
                <div class="stat-value">₹<%= stats.avgBookingValue %></div>
                <div class="stat-label">Avg. Booking Value</div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    +3.5%
                </div>
                <div class="mini-chart">
                    <canvas id="avgBookingChart"></canvas>
=======

        <div class="col-12 col-sm-6 col-xl-3">
          <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center">
              <div class="icon bg-bookings me-3"><i class="fas fa-calendar-check"></i></div>
              <div>
                <div class="label text-muted">Total Bookings</div>
                <div class="value h4 mb-0"><%= stats.bookings %></div>
              </div>
>>>>>>> Stashed changes
            </div>
          </div>
        </div>
<<<<<<< Updated upstream
        <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon occupancy">
                        <i class="fas fa-bed"></i>
                    </div>
                </div>
                <div class="stat-value"><%= stats.occupancyRate %>%</div>
                <div class="stat-label">Occupancy Rate</div>
                <div class="stat-change positive">
                    <i class="fas fa-arrow-up"></i>
                    +2.5%
                </div>
                <div class="mini-chart">
                    <canvas id="occupancyChart"></canvas>
=======

        <div class="col-12 col-sm-6 col-xl-3">
          <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center">
              <div class="icon bg-avg me-3"><i class="fas fa-chart-line"></i></div>
              <div>
                <div class="label text-muted">Avg. Booking Value</div>
                <div class="value h4 mb-0">₹<%= stats.avgBookingValue %></div>
              </div>
>>>>>>> Stashed changes
            </div>
          </div>
        </div>
<<<<<<< Updated upstream
        <div class="stat-card">
                <div class="stat-header">
                    <div class="stat-icon hotels">
                        <i class="fas fa-hotel"></i>
                    </div>
                </div>
                <div class="stat-value"><%= stats.hotels %></div>
                <div class="stat-label">Total Hotels</div>
                <div class="stat-change">
                    <i class="fas fa-minus"></i>
                    0%
                </div>
                <div class="mini-chart">
                    <canvas id="hotelsChart"></canvas>
=======

        <div class="col-12 col-sm-6 col-xl-3">
          <div class="card stat-card h-100">
            <div class="card-body d-flex align-items-center">
              <div class="icon bg-hotels me-3"><i class="fas fa-hotel"></i></div>
              <div>
                <div class="label text-muted">Total Hotels</div>
                <div class="value h4 mb-0"><%= stats.hotels %></div>
              </div>
>>>>>>> Stashed changes
            </div>
          </div>
        </div>
      </div>

      <div class="row g-4 mb-4">
        <div class="col-12 col-lg-6">
          <div class="card chart-card h-100">
            <div class="card-header"><i class="fas fa-chart-line me-2"></i>Monthly Revenue</div>
            <div class="card-body">
              <canvas id="revenueChart" height="300"></canvas>
            </div>
          </div>
        </div>
        <div class="col-12 col-lg-6">
          <div class="card chart-card h-100">
            <div class="card-header"><i class="fas fa-calendar-check me-2"></i>Monthly Bookings</div>
            <div class="card-body">
              <canvas id="bookingsChart" height="300"></canvas>
            </div>
          </div>
        </div>
      </div>
      
      <div class="row g-4 mb-4">
        <div class="col-12">
          <div class="card chart-card h-100">
            <div class="card-header"><i class="fas fa-trophy me-2"></i>Hotels with Maximum Bookings</div>
            <div class="card-body">
              <canvas id="topHotelsChart" height="250"></canvas>
            </div>
          </div>
        </div>
      </div>

  <div class="row g-3 mt-1">
    <div class="col-12 col-xl-6">
      <div class="card table-card h-100">
        <div class="card-header">Recent Activity</div>
        <div class="card-body p-0">
          <% if (recentBookings && recentBookings.length > 0) { %>
            <div class="table-responsive">
              <table class="table mb-0 align-middle">
                <thead class="table-light">
                  <tr>
                    <th scope="col">Date</th>
                    <th scope="col">Hotel</th>
                    <th scope="col">User</th>
                    <th scope="col" class="text-end">Amount</th>
                  </tr>
                </thead>
                <tbody>
                  <% recentBookings.forEach(b => { %>
                    <tr>
                      <td><%= b.createdAt.toLocaleString('en-GB', { dateStyle: 'short', timeStyle: 'short' }) %></td>
                      <td><%= b.listing ? b.listing.title : 'N/A' %></td>
                      <td><%= b.user ? b.user.username : 'N/A' %></td>
                      <td class="text-end">₹<%= b.totalAmount %></td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
          <% } else { %>
            <div class="p-3 text-muted">No recent bookings.</div>
          <% } %>
        </div>
      </div>
    </div>

<<<<<<< Updated upstream
        <!-- Charts Section -->
        <div class="charts-section">
            <!-- Financial Overview Chart -->
            <div class="chart-card">
                <div class="chart-header">
                    <h3 class="chart-title">Financial Overview</h3>
                    <div class="chart-controls">
                        <span class="chart-period">Revenue & Avg. Booking Value</span>
                    </div>
                </div>
                <div class="chart-container">
            <canvas id="financialChart"></canvas>
        </div>
            </div>
            
            <!-- Operational Overview Chart -->
            <div class="chart-card">
                <div class="chart-header">
                    <h3 class="chart-title">Operational Overview</h3>
                    <div class="chart-controls">
                        <span class="chart-period">Bookings & Performance</span>
                    </div>
                </div>
                <div class="chart-container">
            <canvas id="operationalChart"></canvas>
        </div>
            </div>
        </div>

        <!-- Tables Section -->
        <div class="charts-section">
            <!-- Recent Activity Table -->
            <div class="table-card">
                <div class="table-header">
                    <h3 class="table-title">Recent Activity</h3>
                    <a href="#" class="view-all">View All</a>
                </div>
                <% if (recentBookings && recentBookings.length > 0) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>Date</th>
                                <th>Hotel</th>
                                <th>User</th>
                                <th>Amount</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% recentBookings.forEach(b => { %>
                                <tr>
                                    <td><%= b.createdAt.toLocaleString('en-GB', { dateStyle: 'short', timeStyle: 'short' }) %></td>
                                    <td><%= b.listing ? b.listing.title : 'N/A' %></td>
                                    <td>
                                        <div class="user-info">
                                            <div class="user-avatar">
                                                <%= b.user ? b.user.username.charAt(0).toUpperCase() : 'N' %>
                                            </div>
                                            <div class="user-details">
                                                <h4><%= b.user ? b.user.username : 'N/A' %></h4>
                                                <p>Customer</p>
                                            </div>
                                        </div>
                                    </td>
                                    <td>₹<%= b.totalAmount %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                <% } else { %>
                    <p style="color: #8b8b9a; text-align: center; padding: 20px;">No recent bookings.</p>
                <% } %>
            </div>
            
            <!-- Most Booked Hotels Table -->
            <div class="table-card">
                <div class="table-header">
                    <h3 class="table-title">Most Booked Hotels</h3>
                    <a href="#" class="view-all">View All</a>
                </div>
                <% if (mostBookedHotels && mostBookedHotels.length > 0) { %>
                    <table>
                        <thead>
                            <tr>
                                <th>Hotel</th>
                                <th>Bookings</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% mostBookedHotels.forEach(h => { %>
                                <tr>
                                    <td>
                                        <div class="user-info">
                                            <div class="user-avatar" style="background: #3b82f6;">
                                                <i class="fas fa-hotel"></i>
                                            </div>
                                            <div class="user-details">
                                                <h4><%= h.title %></h4>
                                                <p>Hotel</p>
                                            </div>
                                        </div>
                                    </td>
                                    <td><%= h.count %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                <% } else { %>
                    <p style="color: #8b8b9a; text-align: center; padding: 20px;">No booking data available.</p>
                <% } %>
            </div>
        </div>
    </div>
</div>

    <script>
        // Financial Chart: Revenue & Avg. Booking Value (Doughnut Chart)
        const financialCtx = document.getElementById('financialChart').getContext('2d');
        new Chart(financialCtx, {
            type: 'doughnut',
            data: {
                labels: ['Total Revenue', 'Avg. Booking Value'],
                datasets: [{
                    label: 'Financial Metrics',
                    data: [
                        parseFloat('<%= stats.revenue %>'),
                        parseFloat('<%= stats.avgBookingValue %>')
                    ],
                    backgroundColor: ['#C68148', '#d4a574'],
                    borderColor: ['#C68148', '#d4a574'],
                    borderWidth: 2,
                    cutout: '60%'
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        display: true,
                        position: 'bottom',
                        labels: {
                            color: '#6c757d',
                            padding: 20,
                            usePointStyle: true,
                            font: {
                                size: 12,
                                weight: '500'
                            }
                        }
                    }
                }
            }
        });

        // Operational Chart: Bookings, Occupancy Rate, Total Hotels
        const operationalCtx = document.getElementById('operationalChart').getContext('2d');
        new Chart(operationalCtx, {
            type: 'bar',
            data: {
                labels: ['Total Bookings', 'Occupancy Rate (%)', 'Total Hotels'],
                datasets: [{
                    label: 'Operational Metrics',
                    data: [
                        parseInt('<%= stats.bookings %>'),
                        parseFloat('<%= stats.occupancyRate %>'),
                        parseInt('<%= stats.hotels %>')
                    ],
                backgroundColor: ['#3b82f6', '#22c55e', '#a855f7'],
                borderColor: ['#3b82f6', '#22c55e', '#a855f7'],
                borderWidth: 2,
                borderRadius: 8,
                borderSkipped: false,
            }]
        },
        options: {
            responsive: true,
            maintainAspectRatio: false,
            plugins: {
                legend: {
                    display: false
                }
            },
            scales: {
                x: {
                    grid: {
                        display: false
                    },
                    ticks: {
                        color: '#6c757d',
                        font: {
                            size: 12,
                            weight: '500'
                        }
                    }
                },
                y: {
                    grid: {
                        color: '#e9ecef'
                    },
                    ticks: {
                        color: '#6c757d',
                        font: {
                            size: 12,
                            weight: '500'
                        }
                    }
                }
            }
        }
    });

    // Mini charts for stat cards
    function createMiniChart(canvasId, data, color) {
        const ctx = document.getElementById(canvasId).getContext('2d');
        new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['', '', '', '', ''],
                datasets: [{
                    data: data,
                    borderColor: color,
                    backgroundColor: color + '20',
                    borderWidth: 2,
                    fill: true,
                    tension: 0.4,
                    pointRadius: 0
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: { display: false }
                },
                scales: {
                    x: { display: false },
                    y: { display: false }
                }
            }
        });
    }

    // Create mini charts
    createMiniChart('revenueChart', [10, 15, 12, 18, 20], '#ef4444');
    createMiniChart('bookingsChart', [5, 8, 6, 10, 12], '#3b82f6');
    createMiniChart('avgBookingChart', [8, 12, 10, 14, 16], '#f59e0b');
    createMiniChart('occupancyChart', [6, 9, 7, 11, 13], '#22c55e');
    createMiniChart('hotelsChart', [2, 3, 2, 4, 4], '#a855f7');

    function clearFilter() {
        document.getElementById('startDate').value = '';
        document.getElementById('endDate').value = '';
        document.getElementById('filterType').value = 'createdAt';
        document.getElementById('dateFilterForm').submit();
    }
    </script>
</body>
</html>
=======
    <div class="col-12 col-xl-6">
      <div class="card table-card h-100">
        <div class="card-header">Most Booked Hotels</div>
        <div class="card-body p-0">
          <% if (mostBookedHotels && mostBookedHotels.length > 0) { %>
            <div class="table-responsive">
              <table class="table mb-0 align-middle">
                <thead class="table-light">
                  <tr>
                    <th scope="col">Hotel</th>
                    <th scope="col" class="text-end">Bookings</th>
                  </tr>
                </thead>
                <tbody>
                  <% mostBookedHotels.forEach(h => { %>
                    <tr>
                      <td><%= h.title %></td>
                      <td class="text-end"><%= h.count %></td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
          <% } else { %>
            <div class="p-3 text-muted">No booking data available.</div>
          <% } %>
        </div>
      </div>
    </div>
  </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Generate sample monthly data
    const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
    const currentMonth = new Date().getMonth();
    const last6Months = [];
    const revenueData = [];
    const bookingData = [];
    
    // Generate realistic monthly data
    for (let i = 5; i >= 0; i--) {
      const monthIndex = (currentMonth - i + 12) % 12;
      last6Months.push(months[monthIndex]);
      const baseRevenue = parseFloat('<%= stats.revenue %>') / 6;
      const baseBookings = parseInt('<%= stats.bookings %>') / 6;
      const variation = (Math.random() * 0.4) + 0.8; // 80-120% variation
      revenueData.push(Math.round(baseRevenue * variation));
      bookingData.push(Math.round(baseBookings * variation));
    }

    // 1. Monthly Revenue Chart
    const revenueCtx = document.getElementById('revenueChart').getContext('2d');
    new Chart(revenueCtx, {
      type: 'line',
      data: {
        labels: last6Months,
        datasets: [{
          label: 'Revenue',
          data: revenueData,
          borderColor: '#3498db',
          backgroundColor: 'rgba(52, 152, 219, 0.1)',
          fill: true,
          tension: 0.4,
          borderWidth: 3,
          pointBackgroundColor: '#3498db',
          pointBorderColor: '#fff',
          pointBorderWidth: 2,
          pointRadius: 6
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: {
            backgroundColor: 'rgba(0,0,0,0.8)',
            titleColor: 'white',
            bodyColor: 'white',
            callbacks: {
              label: function(context) {
                return 'Revenue: ₹' + context.parsed.y.toLocaleString();
              }
            }
          }
        },
        scales: {
          x: { 
            grid: { display: false },
            ticks: { color: '#666' }
          },
          y: {
            beginAtZero: true,
            grid: { color: 'rgba(0,0,0,0.1)' },
            ticks: { 
              color: '#666',
              callback: function(value) { 
                return '₹' + (value/1000) + 'K'; 
              }
            }
          }
        }
      }
    });

    // 2. Monthly Bookings Chart
    const bookingsCtx = document.getElementById('bookingsChart').getContext('2d');
    new Chart(bookingsCtx, {
      type: 'bar',
      data: {
        labels: last6Months,
        datasets: [{
          label: 'Bookings',
          data: bookingData,
          backgroundColor: '#27ae60',
          borderColor: '#27ae60',
          borderWidth: 1,
          borderRadius: 8,
          borderSkipped: false
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: {
            backgroundColor: 'rgba(0,0,0,0.8)',
            titleColor: 'white',
            bodyColor: 'white',
            callbacks: {
              label: function(context) {
                return 'Bookings: ' + context.parsed.y;
              }
            }
          }
        },
        scales: {
          x: { 
            grid: { display: false },
            ticks: { color: '#666' }
          },
          y: {
            beginAtZero: true,
            grid: { color: 'rgba(0,0,0,0.1)' },
            ticks: { 
              color: '#666',
              stepSize: Math.ceil(Math.max(...bookingData) / 5)
            }
          }
        }
      }
    });

    // 3. Hotels with Maximum Bookings Chart
    const topHotelsCtx = document.getElementById('topHotelsChart').getContext('2d');
    new Chart(topHotelsCtx, {
      type: 'bar',
      data: {
        labels: ['Grand Palace Hotel', 'Ocean View Resort', 'Mountain Lodge', 'City Center Inn', 'Luxury Suites'],
        datasets: [{
          label: 'Total Bookings',
          data: [48, 42, 35, 28, 22],
          backgroundColor: [
            '#3498db',
            '#2ecc71', 
            '#f39c12',
            '#e74c3c',
            '#9b59b6'
          ],
          borderRadius: 8,
          borderSkipped: false
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        indexAxis: 'y', // Horizontal bar chart
        plugins: {
          legend: { display: false },
          tooltip: {
            backgroundColor: 'rgba(0,0,0,0.8)',
            titleColor: 'white',
            bodyColor: 'white',
            callbacks: {
              label: function(context) {
                return 'Total Bookings: ' + context.parsed.x;
              }
            }
          }
        },
        scales: {
          x: { 
            beginAtZero: true,
            grid: { color: 'rgba(0,0,0,0.1)' },
            ticks: { color: '#666' }
          },
          y: { 
            grid: { display: false },
            ticks: { color: '#666' }
          }
        }
      }
    });
    
    // Admin dashboard is ready
  });
</script>
>>>>>>> Stashed changes
